<Page x:Class="STRETCHING.Pages.TrainersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:STRETCHING.Pages"
      mc:Ignorable="d" 
      Style="{StaticResource pageStyle}"
      Title="TrainersPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Управление тренерами" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="#2A503D"
                   HorizontalAlignment="Center"
                   Margin="0 0 0 20"
                   Grid.Row="0"/>

        <!-- Панель управления -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 0 0 20">
            <Button x:Name="AddTrainerButton" Content="Добавить тренера" 
                    Style="{StaticResource MainButton}"
                    Width="150" Height="35" Margin="0 0 10 0" Click="AddTrainerButton_Click"/>
           
            <!--<Button x:Name="RefreshButton" Content="Обновить" 
                    Style="{StaticResource SecondaryButton}"
                    Width="100" Height="35" Click="RefreshButton_Click"/>!-->
        </StackPanel>

        <!-- Список тренеров -->
        <Border Grid.Row="2" Background="Transparent" BorderThickness="0" CornerRadius="6"
                VerticalAlignment="Top"
Height="650">
            <ScrollViewer Background="Transparent" BorderThickness="0">
                <ListBox x:Name="TrainersListBox"
                         Margin="10"
                         
                         Background="Transparent"
                         BorderThickness="0"
                         SelectionMode="Single">

                    <!-- Сброс стандартного стиля ListBoxItem -->
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderBrush" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,0,0,10" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="VerticalContentAlignment" Value="Top" />
                            <Setter Property="Focusable" Value="False" />
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <!-- Шаблон элемента -->
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="15"
                                    Background="White"
                                    BorderBrush="#E0E0E0"
                                    BorderThickness="1"
                                    CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <!-- ФИО и дата рождения -->
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                            <TextBlock FontSize="16" FontWeight="SemiBold"
                                                       Text="{Binding FullName}" 
                                                       Foreground="#2A503D"/>
                                            <TextBlock FontSize="14" Margin="10,0,0,0"
                                                      
                                                       Foreground="#BBA276"/>
                                        </StackPanel>

                                        <!-- Специализации -->
                                        <ItemsControl ItemsSource="{Binding SpecializationNamesList}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}"
                               FontSize="13"
                               Margin="5,0,0,0"
                               Foreground="#2A503D"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <!-- Кнопки действий -->
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" 
                                                VerticalAlignment="Center" HorizontalAlignment="Right">
                                     <!--   <Button Content="Изменить" 
                                                Style="{StaticResource SmallButton}"
                                                Margin="0 0 5 0"
                                                Click="EditTrainerButton_Click"/> !-->
                                        <Button Content="Удалить" 
                                                Style="{StaticResource SmallDangerButton}"
                                                Click="DeleteTrainerButton_Click"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>

